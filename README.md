# README

## Setup

```bash
bundle install       # install gem dependencies
bin/rails db:create  # create the database
bin/rails db:migrate # run migrations
bin/dev              # start the server (and asset watchers)
```
